import React from 'react';

const NotFound = () => (
  <h1 style={{ textAlign: 'center' }}>
    Sorry, page not found
    <span role="img" aria-labelledby="thinking">
      🤔
    </span>
  </h1>
);

export default NotFound;
